apiVersion: v1
kind: Service
metadata:
  name: {{ include "pg-tide.fullname" . }}
  labels:
    {{- include "pg-tide.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "pg-tide.selectorLabels" . | nindent 4 }}
