-- pg_tide 0.7.0 → 0.8.0
-- v0.8.0 is a relay-binary-only release (Slack, Discord, PagerDuty, Arrow Flight sinks).
-- No PostgreSQL catalog changes are required for this version.
-- This script exists to provide a valid upgrade path from 0.7.0 to 0.8.0.

-- (intentionally empty — all changes are in the relay binary)
