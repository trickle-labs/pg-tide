-- pg_tide 0.1.0 → 0.2.0 upgrade script (template)
--
-- To upgrade: ALTER EXTENSION pg_tide UPDATE TO '0.2.0';
--
-- This file is a template showing the pattern for future upgrades.
-- Actual migration statements will be added as features land.

-- Example: Add new columns, tables, or functions here.
-- Always use IF NOT EXISTS / IF EXISTS for idempotency.

-- ── New features in 0.2.0 ─────────────────────────────────────────────────

-- (Placeholder — populate with actual migrations when 0.2.0 work begins)
