schema_version = 1

[[error]]
code = "operator.failure"
surface = "cli"
component = "relay"
summary = "The requested operation could not be completed."
likely_cause = "The relay dependency or catalog state is unavailable."
next_action = "Run `pg-tide doctor` and inspect the relay logs."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = []
since = "v0.47.0"

[[error]]
code = "PGTIDE_EXTENSION_VERSION_INCOMPATIBLE"
surface = "cli"
component = "postgres.extension"
summary = "The installed pg_tide extension is outside the relay lifecycle policy."
likely_cause = "The extension and relay versions are not compatible."
next_action = "Upgrade the extension or relay to an adjacent supported version."
runbook = "failed-upgrade"
retryable = false
cli_exit_code = 1
safe_context_keys = ["installed_version", "relay_version"]
since = "v0.47.0"

[[error]]
code = "PGTIDE_POSTGRES_UNAVAILABLE"
surface = "cli"
component = "postgres"
summary = "PostgreSQL is unavailable."
likely_cause = "The database is unreachable or refused the connection."
next_action = "Check PostgreSQL reachability and retry the command."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_POSTGRES_AUTHENTICATION"
surface = "cli"
component = "postgres"
summary = "PostgreSQL authentication was rejected."
likely_cause = "The configured database credentials are invalid or expired."
next_action = "Check the URL file or secret and retry with valid credentials."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_POSTGRES_AUTHORIZATION"
surface = "cli"
component = "postgres"
summary = "PostgreSQL denied the requested operation."
likely_cause = "The database role lacks a required schema or table privilege."
next_action = "Grant the documented pg_tide privileges and retry."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_CATALOG_MISSING"
surface = "cli"
component = "postgres.catalog"
summary = "The pg_tide catalog is missing."
likely_cause = "The extension has not been installed or its migration is incomplete."
next_action = "Install or upgrade pg_tide, then run `pg-tide doctor` again."
runbook = "failed-upgrade"
retryable = false
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_PIPELINE_NOT_FOUND"
surface = "cli"
component = "postgres.catalog"
summary = "The requested pipeline was not found."
likely_cause = "The pipeline name is absent from the PostgreSQL catalog."
next_action = "Check the pipeline name and configure it before retrying."
runbook = "pipeline-undiscovered"
retryable = false
cli_exit_code = 1
safe_context_keys = ["pipeline"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_PIPELINE_INVALID"
surface = "cli"
component = "relay.pipeline"
summary = "The pipeline configuration is invalid."
likely_cause = "A required pipeline field or secret reference is invalid."
next_action = "Run `pg-tide config validate` and correct the reported configuration."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["pipeline"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_PIPELINE_NOT_DISCOVERED"
surface = "cli"
component = "relay.pipeline"
summary = "The pipeline was not discovered by the relay."
likely_cause = "The catalog row is absent, disabled, or outside the relay tenant."
next_action = "Check catalog configuration, tenant ownership, and relay logs."
runbook = "pipeline-undiscovered"
retryable = false
cli_exit_code = 1
safe_context_keys = ["pipeline"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_PIPELINE_NOT_OWNED"
surface = "cli"
component = "relay.pipeline"
summary = "The relay does not own the requested pipeline."
likely_cause = "Another relay holds the advisory lock or tenant ownership is different."
next_action = "Check relay group and tenant ownership before retrying."
runbook = "ownership-ambiguity"
retryable = true
cli_exit_code = 1
safe_context_keys = ["pipeline"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_UNAVAILABLE"
surface = "cli"
component = "connector"
summary = "The destination connector is unavailable."
likely_cause = "The destination service is unreachable or not ready."
next_action = "Check destination health and retry the operation."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_TIMEOUT"
surface = "cli"
component = "connector"
summary = "The destination connector timed out."
likely_cause = "The destination exceeded its configured request timeout."
next_action = "Check destination latency and retry the operation."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_THROTTLED"
surface = "cli"
component = "connector"
summary = "The destination connector throttled the request."
likely_cause = "The destination rate limit was exceeded."
next_action = "Wait for the destination rate limit to recover, then retry."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_AUTHENTICATION"
surface = "cli"
component = "connector"
summary = "The destination connector rejected authentication."
likely_cause = "The destination credential is invalid, expired, or unavailable."
next_action = "Refresh the destination credential and validate the pipeline."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_AUTHORIZATION"
surface = "cli"
component = "connector"
summary = "The destination connector denied authorization."
likely_cause = "The destination identity lacks permission for the target."
next_action = "Grant the required destination permission and retry."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_TLS_VERIFICATION_FAILED"
surface = "cli"
component = "connector"
summary = "TLS verification failed."
likely_cause = "The certificate, trust store, hostname, or TLS mode is invalid."
next_action = "Check the CA and hostname configuration; do not disable verification."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_WEBHOOK_SSRF_REJECTED"
surface = "cli"
component = "connector.webhook"
summary = "The webhook destination was rejected by SSRF protection."
likely_cause = "The endpoint resolves to a private, loopback, link-local, or otherwise blocked address."
next_action = "Use an approved public HTTPS endpoint or review the webhook policy."
runbook = "webhook-authentication-failure"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_INVALID_DESTINATION"
surface = "cli"
component = "connector"
summary = "The destination address or topic is invalid."
likely_cause = "The configured destination does not exist or violates connector rules."
next_action = "Correct the destination and run `pg-tide config validate`."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_MESSAGE_TOO_LARGE"
surface = "cli"
component = "connector"
summary = "The connector rejected a message that exceeded its limit."
likely_cause = "The batch or message is larger than the destination limit."
next_action = "Reduce message or batch size and retry."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_PROTOCOL_REJECTION"
surface = "cli"
component = "connector"
summary = "The destination rejected the request protocol."
likely_cause = "The destination schema, topic, or request format is incompatible."
next_action = "Check the destination contract and pipeline configuration."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_INVALID_CONFIG"
surface = "cli"
component = "connector"
summary = "The connector configuration is invalid."
likely_cause = "A required destination setting is missing or malformed."
next_action = "Run `pg-tide config validate` and correct the connector settings."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONNECTOR_UNKNOWN"
surface = "cli"
component = "connector"
summary = "The destination connector failed unexpectedly."
likely_cause = "The destination returned an unclassified failure."
next_action = "Run `pg-tide doctor`, inspect the connector logs, and retry if safe."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = 1
safe_context_keys = ["connector"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_REPLAY_INPUT_INVALID"
surface = "cli"
component = "relay.replay"
summary = "The replay or DLQ input is invalid."
likely_cause = "The pipeline, ID range, or DLQ key does not satisfy replay requirements."
next_action = "Check replay arguments and use a bounded valid range."
runbook = "dlq-growth"
retryable = false
cli_exit_code = 1
safe_context_keys = ["pipeline"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_MAINTENANCE_SWEEP_FAILED"
surface = "cli"
component = "postgres.maintenance"
summary = "The maintenance sweep could not be completed."
likely_cause = "The catalog or outbox cleanup operation failed."
next_action = "Check PostgreSQL health and retry the bounded sweep."
runbook = "cleanup-failure"
retryable = true
cli_exit_code = 1
safe_context_keys = ["outbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_SHUTDOWN_FAILED"
surface = "cli"
component = "relay.shutdown"
summary = "The relay could not complete graceful shutdown."
likely_cause = "An in-flight operation or coordination channel closed unexpectedly."
next_action = "Inspect relay logs and verify that ownership was released before restarting."
runbook = "ownership-ambiguity"
retryable = true
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_SUPPORT_BUNDLE_WRITE_FAILED"
surface = "cli"
component = "support.bundle"
summary = "The support bundle could not be written."
likely_cause = "The output directory is missing or is not writable."
next_action = "Choose a writable empty directory and retry the diagnostic command."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_INTERNAL_FAILURE"
surface = "cli"
component = "relay"
summary = "The relay encountered an internal failure."
likely_cause = "An unexpected error occurred outside a classified operational path."
next_action = "Run `pg-tide doctor`, collect redacted diagnostics, and contact the maintainers."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = 1
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_CONFIG_UNSUPPORTED_SURFACE"
surface = "sql"
component = "postgres.catalog"
summary = "The requested configuration surface is not supported."
likely_cause = "The function targets a removed or unsupported v1 surface."
next_action = "Use the documented supported SQL API alternative."
runbook = "failed-upgrade"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["surface"]
since = "v0.48.0"

[[error]]
code = "PGTIDE_OUTBOX_ALREADY_EXISTS"
surface = "sql"
component = "postgres.outbox"
summary = "The outbox already exists."
likely_cause = "A create operation used an existing outbox name."
next_action = "Choose a new name or use the existing outbox."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["outbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_OUTBOX_NOT_FOUND"
surface = "sql"
component = "postgres.outbox"
summary = "The outbox was not found."
likely_cause = "The requested outbox name is absent from the catalog."
next_action = "Check the outbox name and create it before retrying."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["outbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_INBOX_ALREADY_EXISTS"
surface = "sql"
component = "postgres.inbox"
summary = "The inbox already exists."
likely_cause = "A create operation used an existing inbox name."
next_action = "Choose a new name or use the existing inbox."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["inbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_INBOX_NOT_FOUND"
surface = "sql"
component = "postgres.inbox"
summary = "The inbox was not found."
likely_cause = "The requested inbox name is absent from the catalog."
next_action = "Check the inbox name and create it before retrying."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["inbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_RELAY_NOT_FOUND"
surface = "sql"
component = "postgres.catalog"
summary = "The relay pipeline was not found."
likely_cause = "The requested relay name is absent from the catalog."
next_action = "Check the relay name and configure it before retrying."
runbook = "pipeline-undiscovered"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["pipeline"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_INVALID_ARGUMENT"
surface = "sql"
component = "postgres.validation"
summary = "A SQL function argument is invalid."
likely_cause = "A required identifier, range, or option failed validation."
next_action = "Correct the argument using the documented SQL constraints."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = []
since = "v0.54.0"

[[error]]
code = "PGTIDE_SWEEP_FAILED"
surface = "sql"
component = "postgres.maintenance"
summary = "The outbox sweep failed."
likely_cause = "The cleanup query or catalog state could not complete."
next_action = "Check PostgreSQL health and retry the bounded sweep."
runbook = "cleanup-failure"
retryable = true
cli_exit_code = "null"
safe_context_keys = ["outbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_PUBLISH_DENIED"
surface = "sql"
component = "postgres.authorization"
summary = "Publishing to the outbox was denied."
likely_cause = "The current role lacks the configured publish privilege."
next_action = "Grant publish permission to the role or use an authorized role."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["outbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_AUTHORIZATION_FAILED"
surface = "sql"
component = "postgres.authorization"
summary = "The PostgreSQL authorization check failed."
likely_cause = "The current role or catalog privilege is insufficient."
next_action = "Review the pg_tide grants and retry with an authorized role."
runbook = "relay-will-not-start"
retryable = false
cli_exit_code = "null"
safe_context_keys = ["outbox"]
since = "v0.54.0"

[[error]]
code = "PGTIDE_SPI_ERROR"
surface = "sql"
component = "postgres.catalog"
summary = "The PostgreSQL catalog operation failed."
likely_cause = "The database returned an internal or unexpected catalog error."
next_action = "Inspect the PostgreSQL error context and verify catalog health."
runbook = "relay-will-not-start"
retryable = true
cli_exit_code = "null"
safe_context_keys = []
since = "v0.54.0"
